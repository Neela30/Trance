"""TRANCE desktop GUI (PySide6): Analyse / Report / History tabs over the
existing main.run_pipeline()/report.py pipeline -- see gui_main.py for the
entrypoint. Nothing in this package changes CLI behavior (main.py,
analyze_evidence.py, acquire_all.py); it's an additional way to drive the
same backend, not a replacement.
"""
